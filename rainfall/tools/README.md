# Rainfall tools

Reusable helper scripts used during multiple Rainfall levels.

- `pattern_generator.py`: generates cyclic patterns and finds crash offsets from EIP-style values.
- `exploit.py`: small payload helper/script kept as a reusable exploitation aid.

These scripts are intentionally lightweight and kept separate from level folders to
avoid duplicating helper logic across challenges.
